module.exports = {
  // Content configuration for automatic content detection
  content: [
    './_includes/**/*.html',
    './_layouts/**/*.html',
    './*.{html,md}',
    './assets/js/**/*.js'
  ],
  // Theme configuration moved to CSS @theme block in src/stylev3.css
  // Plugins are now imported via @plugin directives in CSS
}
